player = {
    x = PLAYER_SPAWN_X,
    y = PLAYER_SPAWN_Y,
    current_sprite = PLAYER_SPRITE_RIGHT;
    w = 8,
    h = 8,
    hp = PLAYER_MAX_HP,
    max_hp = PLAYER_MAX_HP,
    dx = 0, -- Player velocity X
    dy = 0,  -- Player velocity Y
    speed = PLAYER_SPEED
}
